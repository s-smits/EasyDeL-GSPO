commit 73984a8838acbafc23ac90538d236cf375e6501f
Author: s-smits <sjssmits@gmail.com>
Date:   Thu Aug 28 01:42:31 2025 +0200

    Streamline: remove redundant ref-sync flags from barriers mode (sync_ref_model=false makes them unused)

diff --git a/progress.txt b/progress.txt
index b7fd8167..38f5769e 100644
--- a/progress.txt
+++ b/progress.txt
@@ -254,3 +254,4 @@ Verdict: Functionally correct. Minor: could add explicit ref_model_sync_steps to
 - Added multi-host-safe alignment check in GRPOTrainer:on_step_start: aggregates MAD across hosts when barriers enabled and takes unified decision.
 - In run script, barriers mode now sets: --logprob_analysis_enable true, --logprob_alignment_check_on_sync true.
 - Ref mode stays with diagnostics disabled to save memory.
+[] Streamlined barriers SYNC_FLAGS: removed redundant ref-sync flags (steps/on_start/copy_graphother) when sync_ref_model=false.
diff --git a/run_gspo_training.sh b/run_gspo_training.sh
index adf071f5..9b196331 100755
--- a/run_gspo_training.sh
+++ b/run_gspo_training.sh
@@ -54,13 +54,10 @@ echo "LOG_GLOBAL: ${LOG_GLOBAL_VAL}"
 
 # Compute sync flags based on SYNC_MODE (mutually exclusive)
 if [ "$SYNC_MODE" = "barriers" ]; then
-  # Multi-host barriers only; explicitly disable all ref-sync toggles
+  # Multi-host barriers only; ref-sync disabled, diagnostics enabled
   SYNC_FLAGS="\
   --sync_multihost_phases true \
   --sync_ref_model false \
-  --ref_model_sync_steps 64 \
-  --sync_ref_model_on_step_start false \
-  --ref_sync_copy_graphother false \
   --logprob_alignment_check_on_sync true \
   --logprob_analysis_enable true"
 else
