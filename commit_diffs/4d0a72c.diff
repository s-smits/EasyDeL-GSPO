commit 4d0a72c8f0b2113b4e9d7857857c9f70f47867b7
Author: s-smits <sjssmits@gmail.com>
Date:   Tue Aug 26 13:25:28 2025 +0000

    Remove unused GSPOConfig.epsilon_coupling_mode; consolidate stable trainer naming; keep back-compat alias; update script selection and config flag; wire metrics and runtime scalars; minor cleanups.

diff --git a/easydel/trainers/group_relative_policy_optimization/gspo_config.py b/easydel/trainers/group_relative_policy_optimization/gspo_config.py
index f0dec869..91bb6db9 100644
--- a/easydel/trainers/group_relative_policy_optimization/gspo_config.py
+++ b/easydel/trainers/group_relative_policy_optimization/gspo_config.py
@@ -67,11 +67,6 @@ class GSPOConfig(GRPOConfig):
         metadata={"help": "Absolute cap for sequence log-ratio before exponentiation when clip_in_log_space=True."},
     )
 
-    # Epsilon coupling strategy (static, JIT-safe): none|k_over_g|ess_over_g
-    epsilon_coupling_mode: str = field(
-        default="k_over_g",
-        metadata={"help": "How to scale PPO epsilon at runtime: 'none'|'k_over_g'|'ess_over_g'."},
-    )
 
     def __post_init__(self):
         """Post initialization to set dependent parameters."""
@@ -93,10 +88,6 @@ class GSPOConfig(GRPOConfig):
                 raise ValueError(
                     "importance_weight_normalization must be one of ['none','mean','ess']"
                 )
-            if self.epsilon_coupling_mode not in ["none", "k_over_g", "ess_over_g"]:
-                raise ValueError(
-                    "epsilon_coupling_mode must be one of ['none','k_over_g','ess_over_g']"
-                )
             if float(self.log_clip_epsilon) <= 0:
                 raise ValueError("log_clip_epsilon must be positive")
 
